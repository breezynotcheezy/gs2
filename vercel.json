{
  "version": 2,
  "builds": [
    {
      "src": "front/package.json",
      "use": "@vercel/next",
      "config": {
        "outputFileTracing": true
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/front" }
  ]
}
